# ==================== API 配置 ====================

# GLM (智谱AI) API 配置
# 获取地址：https://open.bigmodel.cn/usercenter/apikeys
GLM_API_KEY=your_glm_api_key_here
GLM_MODEL=glm-4
GLM_TEMPERATURE=0.7
GLM_TOP_P=0.9
GLM_MAX_TOKENS=2000
GLM_TIMEOUT=30000
GLM_STREAM_TIMEOUT=60000

# DeepSeek API 配置
# 获取地址：https://platform.deepseek.com/api-keys
DEEPSEEK_API_KEY=your_deepseek_api_key_here
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_TEMPERATURE=0.7
DEEPSEEK_MAX_TOKENS=2000
DEEPSEEK_TIMEOUT=30000
DEEPSEEK_STREAM_TIMEOUT=60000

# 默认使用的模型 (glm 或 deepseek)
DEFAULT_MODEL=glm

# ==================== 服务器配置 ====================

# 服务器端口
PORT=3000

# 节点环境 (development / production)
NODE_ENV=development

# ==================== 安全配置 ====================

# CORS 允许的来源（逗号分隔）
# 示例：http://localhost:3000,https://yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ==================== 速率限制 ====================

# 速率限制（15分钟内每个IP最多请求次数）
RATE_LIMIT_MAX=100

# ==================== 消息验证 ====================

# 最大消息长度
MAX_MESSAGE_LENGTH=10000
